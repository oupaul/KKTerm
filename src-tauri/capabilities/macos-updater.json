{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-updater",
  "description": "Allow the main window to check, download, install, and relaunch signed macOS app updates.",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
