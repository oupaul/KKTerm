# dashboard.deleteViewBody

- **English value**: `Delete "{{name}}" and all widgets in this Dashboard view? This cannot be undone.`
- **Namespace**: `dashboard`
- **File/component**: `src/dashboard/DashboardPage.tsx`
- **UI role**: `fragment`
- **User flow**: `Shown in the delete confirmation dialog after a user clicks a Dashboard View close button while editing the Dashboard layout. The dialog confirms deletion before removing the durable Dashboard View and its Widget Instances.`
- **Tone**: `concise/neutral warning`
- **Placeholders**: `{{name}}` is the Dashboard View title.
- **Domain notes**: `Dashboard View is a durable SQLite-backed view inside the Dashboard module. Dashboard is the widget module, not the default launch state.`

<!--
Filename: dashboard.deleteViewBody.md (e.g. ai.dashboardToolsDisabledTitle.md)
Delete this file once every non-English locale under src/i18n/locales/ has the key translated.
-->
