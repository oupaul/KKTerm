# settings.useCursorCli

- **English value**: `Use Cursor Agent CLI`
- **Namespace**: `settings`
- **File/component**: `src/modules/settings/AiSettings.tsx`
- **UI role**: label
- **User flow**: Shown in Settings → AI when the Cursor provider is selected, as the toggle to route the AI Assistant through the local Cursor Agent CLI.
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: Toggle label for enabling the local Cursor Agent CLI backend (not the Cursor IDE editor, and not a media “play” or other CLI product).
- **Domain notes**: Keep “Cursor Agent CLI” as the product/command family name; do not translate it into a generic “cursor” UI pointer term.
