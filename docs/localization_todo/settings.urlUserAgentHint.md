# Pending localization: settings.urlUserAgentHint

English source:

```json
"urlUserAgentHint": "Choose the default user-agent string for URL Connections. Leave blank to use each platform webview default; paste a custom value when a site requires one."
```

Follow `docs/localization_todo/README.md` and complete every supported locale before deleting this file.
