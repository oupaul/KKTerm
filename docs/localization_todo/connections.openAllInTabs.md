# connections.openAllInTabs

- **English value**: `In separate Tabs`
- **Namespace**: `connections`
- **File/component**: `src/modules/workspace/connections/ConnectionSidebar.tsx`
- **UI role**: `button`
- **User flow**: Submenu entry under the folder right-click action "Open all connections". Opens every Connection in the folder, each in its own Tab (the standard open path).
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: Reads as a continuation of the parent label "Open all connections" → "In separate Tabs". "Tab" is the frontend Workspace container.
- **Domain notes**: "Tab" uses the locale's established tab term (e.g. zh-TW 分頁 / zh-CN 标签页). Best-effort translations were added to every non-English locale; review before deleting this file.
